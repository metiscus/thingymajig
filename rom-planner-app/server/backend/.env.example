# backend/.env.example

# Database URL will be constructed from these in podman-compose.yaml
# and passed as DATABASE_URL to the container.
POSTGRES_DB=rom_planner_db
POSTGRES_USER=rom_user
POSTGRES_PASSWORD=securepassword

# Example for CORS origins - passed from podman-compose environment
FRONTEND_CORS_ORIGINS=http://localhost:5173

# UVICORN_RELOAD=true # Uncomment for FastAPI hot-reloading (requires specific uvicorn command)